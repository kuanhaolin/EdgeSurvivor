# Story 1.6: 個人統計儀表板

## Status

Draft

## Story

**As a** 已登入使用者,
**I want** 查看我的個人儀表板，包含我發起/參與的活動數、媒合數及平均評價,
**so that** 我可以快速了解我的平台活動概況。

## Acceptance Criteria

### 儀表板顯示

1. 使用者可以存取個人儀表板頁面
2. 儀表板應顯示以下統計數據：
   - 總活動數（包含創建的和參與的活動）
   - 媒合成功數量
   - 收到的評價數量
   - 平均評分（如有評價）
3. 每個統計數據應有圖示和清楚的標籤
4. 統計數據應使用卡片式設計呈現
5. 數據應在頁面載入時自動獲取

### 最近活動列表

6. 儀表板應顯示「最近活動」區塊
7. 顯示最多 5 個最近的活動（包含創建和參與的）
8. 每個活動應顯示：標題、日期、地點、狀態
9. 點擊活動卡片可跳轉至活動詳情頁
10. 若無活動，顯示空狀態提示與「建立活動」按鈕

### 快速操作

11. 提供「建立新活動」快速按鈕
12. 提供「瀏覽活動」快速按鈕
13. 提供「我的媒合」快速按鈕

### 效能與體驗

14. 統計數據載入時顯示骨架屏（Skeleton）
15. API 回應時間需低於 500ms
16. 數據應即時更新（無需手動刷新）
17. 頁面應響應式設計，適配手機與平板

### 錯誤處理

18. 網路錯誤時顯示友善的錯誤訊息
19. 提供重新載入按鈕
20. 認證失效時自動跳轉至登入頁

## Tasks / Subtasks

> **🔧 Brownfield 專案說明**：後端已實作統計 API (`/api/users/stats`) 和最近活動 API (`/api/users/recent-activities`)。本 Story 著重於**建立前端儀表板頁面**、**整合現有 API**，以及**設計優質的使用者體驗**。

### Phase 1: 後端 API 驗證

- [ ] **Task 1: 驗證統計 API** (AC: 2, 15)
  - [ ] ✅ 確認 `backend/blueprints/users.py` 中的 `get_user_stats()` 端點存在
  - [ ] ✅ 驗證端點路徑為 `GET /api/users/stats`
  - [ ] ✅ 檢查需要 JWT 認證 (`@jwt_required()`)
  - [ ] ✅ 驗證回應格式包含：
    - `activities` (總活動數)
    - `matches` (媒合成功數)
    - `reviews` (評價數量)
    - `travelDays` (旅遊天數，可選)
  - [ ] ✅ 確認統計計算邏輯正確
  - [ ] ✅ 測試 API 回應時間 < 500ms
  - [ ] 📝 記錄任何與規格不符的地方

- [ ] **Task 2: 驗證最近活動 API** (AC: 6, 7, 8, 15)
  - [ ] ✅ 確認 `backend/blueprints/users.py` 中的 `get_recent_activities()` 端點存在
  - [ ] ✅ 驗證端點路徑為 `GET /api/users/recent-activities`
  - [ ] ✅ 檢查需要 JWT 認證
  - [ ] ✅ 驗證回應格式包含活動陣列（最多 5 個）
  - [ ] ✅ 確認活動包含必要欄位：
    - `activity_id`, `title`, `start_date`, `location`, `status`
  - [ ] ✅ 驗證活動排序（最新的在前）
  - [ ] ✅ 測試 API 回應時間 < 500ms
  - [ ] 📝 記錄任何與規格不符的地方

### Phase 2: 前端頁面建立

- [ ] **Task 3: 建立 Dashboard.vue 頁面** (AC: 1, 17)
  - [ ] ➕ 在 `frontend/src/views/` 建立或更新 `Dashboard.vue`
  - [ ] ➕ 使用 Composition API 架構
  - [ ] ➕ 設計頁面佈局：
    - 頁面標題「我的儀表板」
    - 統計卡片區（4 個統計卡片）
    - 快速操作區
    - 最近活動列表區
  - [ ] ➕ 添加導航守衛（需登入才能存取）
  - [ ] ➕ 設計響應式佈局（使用 Grid 或 Flex）
  - [ ] ➕ 添加骨架屏（Loading Skeleton）

- [ ] **Task 4: 實作統計卡片組件** (AC: 2, 3, 4, 14)
  - [ ] ➕ 建立 `StatCard.vue` 組件（或在 Dashboard.vue 內定義）
  - [ ] ➕ 組件應接收 Props：
    - `icon` (圖示)
    - `label` (標籤文字)
    - `value` (統計數值)
    - `color` (主題顏色，可選)
  - [ ] ➕ 設計卡片樣式：
    - 使用 Element Plus Card 組件
    - 圖示使用 Element Plus Icons
    - 數值使用大字體顯示
    - 添加動畫效果
  - [ ] ➕ 添加 Skeleton 載入狀態
  - [ ] ➕ 統計卡片包含：
    - 活動數 (Calendar 圖示)
    - 媒合數 (UserFilled 圖示)
    - 評價數 (Star 圖示)
    - 平均評分 (Trophy 圖示，若有評價)

- [ ] **Task 5: 實作最近活動列表** (AC: 6, 7, 8, 9, 10)
  - [ ] ➕ 在 Dashboard.vue 建立「最近活動」區塊
  - [ ] ➕ 使用 ActivityCard 組件顯示活動（或建立簡化版）
  - [ ] ➕ 每個活動卡片顯示：
    - 活動標題
    - 開始日期（格式化）
    - 地點
    - 狀態標籤（使用 ElTag）
  - [ ] ➕ 實作點擊跳轉至活動詳情
  - [ ] ➕ 實作空狀態組件：
    - 顯示空狀態圖示
    - 顯示提示文字「還沒有任何活動」
    - 顯示「建立活動」按鈕

- [ ] **Task 6: 實作快速操作區** (AC: 11, 12, 13)
  - [ ] ➕ 建立快速操作區塊
  - [ ] ➕ 添加三個快速操作按鈕：
    - 「建立新活動」(跳轉至 /activities/create)
    - 「瀏覽活動」(跳轉至 /activities)
    - 「我的媒合」(跳轉至 /matches)
  - [ ] ➕ 使用 Element Plus Button 組件
  - [ ] ➕ 添加圖示與文字
  - [ ] ➕ 設計按鈕樣式與排列

### Phase 3: API 服務整合

- [ ] **Task 7: 擴展 Users API 服務** (AC: 5, 16)
  - [ ] ➕ 在 `frontend/src/api/users.js` 添加或確認 `getUserStats()` 函數
  - [ ] ➕ 函數應調用 `GET /api/users/stats`
  - [ ] ➕ 添加或確認 `getRecentActivities()` 函數
  - [ ] ➕ 函數應調用 `GET /api/users/recent-activities`
  - [ ] ➕ 返回 Promise with response data

- [ ] **Task 8: 實作儀表板資料獲取邏輯** (AC: 5, 16, 18, 19, 20)
  - [ ] ➕ 建立 `fetchDashboardData` 函數
  - [ ] ➕ 並行調用 `getUserStats()` 和 `getRecentActivities()`
  - [ ] ➕ 使用 `Promise.all()` 優化載入速度
  - [ ] ➕ 實作載入狀態管理（`loading` ref）
  - [ ] ➕ 實作錯誤處理：
    - 捕獲 API 錯誤
    - 顯示錯誤訊息
    - 提供重新載入按鈕
  - [ ] ➕ 處理認證失效（401）：自動跳轉至登入頁
  - [ ] ➕ 在 `onMounted` 生命週期自動載入資料

### Phase 4: UI/UX 優化

- [ ] **Task 9: 實作骨架屏載入效果** (AC: 14)
  - [ ] ➕ 使用 Element Plus Skeleton 組件
  - [ ] ➕ 為統計卡片建立 Skeleton
  - [ ] ➕ 為活動列表建立 Skeleton
  - [ ] ➕ 載入完成後切換至實際內容
  - [ ] ➕ 添加淡入動畫

- [ ] **Task 10: 實作數據格式化** (AC: 8)
  - [ ] ➕ 格式化日期顯示（使用 Day.js）
  - [ ] ➕ 格式化數字顯示（千分位分隔符，如適用）
  - [ ] ➕ 計算平均評分（若有評價）
  - [ ] ➕ 處理空數據情況（顯示 0 或 "N/A"）

- [ ] **Task 11: 實作響應式設計** (AC: 17)
  - [ ] ➕ 使用 CSS Grid 或 Flex 佈局
  - [ ] ➕ 桌面版：4 欄統計卡片
  - [ ] ➕ 平板版：2 欄統計卡片
  - [ ] ➕ 手機版：1 欄統計卡片
  - [ ] ➕ 測試各種螢幕尺寸
  - [ ] ➕ 確保所有元素在小螢幕上可讀

- [ ] **Task 12: 實作錯誤處理與重新載入** (AC: 18, 19)
  - [ ] ➕ 建立錯誤狀態組件
  - [ ] ➕ 顯示錯誤圖示與訊息
  - [ ] ➕ 添加「重新載入」按鈕
  - [ ] ➕ 實作重新載入邏輯
  - [ ] ➕ 處理不同類型的錯誤：
    - 網路錯誤
    - 伺服器錯誤
    - 認證錯誤

### Phase 5: 路由與導航

- [ ] **Task 13: 路由配置** (AC: 1, 20)
  - [ ] ➕ 在 `frontend/src/router/index.js` 確認儀表板路由
  - [ ] ➕ 路由配置：
    - Path: `/dashboard`
    - Component: `Dashboard.vue`
    - Meta: `{ requiresAuth: true, title: '我的儀表板' }`
  - [ ] ➕ 添加路由守衛（需登入）
  - [ ] ➕ 設定為登入後的預設頁面（可選）
  - [ ] ➕ 測試路由導航功能

- [ ] **Task 14: 導航連結整合** (AC: 1)
  - [ ] ➕ 在導航欄添加「儀表板」連結
  - [ ] ➕ 確認從其他頁面可跳轉至儀表板
  - [ ] ➕ 登入成功後跳轉至儀表板（可選）
  - [ ] ➕ 驗證導航功能正常

### Phase 6: 測試與驗證

- [ ] **Task 15: 前端測試建立** (AC: 2, 3, 4, 8)
  - [ ] ➕ 在 `frontend/tests/unit/views/` 建立 `Dashboard.spec.js`
  - [ ] ➕ 實作測試案例：
    - [ ] 測試頁面正確渲染
    - [ ] 測試統計卡片顯示
    - [ ] 測試最近活動列表渲染
    - [ ] 測試空狀態顯示
    - [ ] 測試快速操作按鈕
    - [ ] 測試資料獲取（Mock API）
    - [ ] 測試載入狀態
    - [ ] 測試錯誤處理
    - [ ] 測試點擊跳轉邏輯
  - [ ] ➕ Mock `getUserStats` 和 `getRecentActivities` API
  - [ ] 🧪 執行測試：`npm run test:unit`
  - [ ] 📊 確認覆蓋率 > 80%

- [ ] **Task 16: 後端測試補充** (AC: 2, 7, 15)
  - [ ] ➕ 在 `backend/tests/test_users.py` 建立或擴展統計測試
  - [ ] ➕ 測試案例：
    - [ ] 測試獲取統計數據成功
    - [ ] 測試統計計算正確性（活動數、媒合數）
    - [ ] 測試獲取最近活動成功
    - [ ] 測試最近活動排序與限制（最多 5 個）
    - [ ] 測試未認證請求（應返回 401）
    - [ ] 測試無活動時的回應
  - [ ] 🧪 執行測試：`pytest backend/tests/test_users.py -v`
  - [ ] 📊 確認所有測試通過

### Phase 7: 整合測試

- [ ] **Task 17: 端到端功能驗證** (所有 AC)
  - [ ] 🧪 手動測試清單：
    - [ ] **測試 1**: 完整儀表板載入流程
      - 登入帳號
      - 訪問 http://localhost:8080/dashboard
      - 驗證：統計卡片顯示正確數據
      - 驗證：最近活動列表顯示
      - 驗證：快速操作按鈕存在
    - [ ] **測試 2**: 統計數據準確性
      - 建立新活動
      - 重新載入儀表板
      - 驗證：活動數增加
    - [ ] **測試 3**: 最近活動點擊
      - 點擊活動卡片
      - 驗證：跳轉至活動詳情頁
    - [ ] **測試 4**: 快速操作按鈕
      - 點擊「建立新活動」
      - 驗證：跳轉至建立活動頁
      - 點擊「瀏覽活動」
      - 驗證：跳轉至活動列表頁
      - 點擊「我的媒合」
      - 驗證：跳轉至媒合頁
    - [ ] **測試 5**: 空狀態顯示
      - 使用無活動的測試帳號
      - 訪問儀表板
      - 驗證：顯示空狀態提示
      - 驗證：顯示「建立活動」按鈕
    - [ ] **測試 6**: 載入狀態
      - 訪問儀表板
      - 驗證：顯示骨架屏
      - 驗證：載入完成後切換至實際內容
    - [ ] **測試 7**: 錯誤處理
      - 模擬網路錯誤（關閉後端）
      - 訪問儀表板
      - 驗證：顯示錯誤訊息
      - 驗證：顯示重新載入按鈕
      - 啟動後端並點擊重新載入
      - 驗證：資料正常載入
    - [ ] **測試 8**: 認證保護
      - 登出狀態訪問 /dashboard
      - 驗證：跳轉至登入頁
    - [ ] **測試 9**: 響應式設計
      - 在桌面模式測試（4 欄卡片）
      - 在平板模式測試（2 欄卡片）
      - 在手機模式測試（1 欄卡片）
      - 驗證：所有佈局正確顯示
    - [ ] **測試 10**: 效能測試
      - 使用 DevTools Network 面板
      - 驗證：API 回應時間 < 500ms
      - 驗證：頁面載入流暢
  - [ ] 📊 驗證所有 AC 滿足
  - [ ] 📝 記錄所有測試結果

### 圖例說明
- ✅ **已存在且正確** - 驗證通過即可
- ⚠️ **需要檢查** - 可能需要調整
- ➕ **需要新增** - 缺失的功能
- 🔄 **需要重構** - 需要改進現有實作
- 📝 **記錄** - 文件化發現
- 🧪 **測試** - 執行測試
- 📊 **驗證** - 確認符合需求

## Dev Notes

### Previous Story Insights

**來自 Story 1.1 - 1.5 的經驗**：
- ✅ Vue 3 Composition API 是標準開發模式
- ✅ Element Plus 組件庫已整合且運作良好
- ✅ Pinia Auth Store 已建立，用於管理認證狀態
- ✅ JWT 認證機制完善（`@jwt_required()` 裝飾器）
- ✅ Axios 攔截器已配置，處理認證和錯誤
- ✅ 路由守衛機制已建立（`requiresAuth` meta）
- ✅ 響應式設計模式已確立
- ✅ 日期格式化使用 Day.js
- ✅ 錯誤處理模式：後端返回具體錯誤，前端顯示友善訊息

**Brownfield 專案背景**：
- 後端統計 API 已完整實作（`/api/users/stats`, `/api/users/recent-activities`）
- 主要工作是**建立前端儀表板頁面**和**整合現有 API**
- 需要設計優質的使用者體驗（載入狀態、錯誤處理、空狀態）

### Data Models

**User Model** [Source: backend/models/user.py]
- **Table**: `users`
- **Primary Key**: `user_id` (Integer, Auto-increment)
- **統計相關欄位**:
  - `rating_count` (Integer) - 收到的評價數量
  - `average_rating` (Float) - 平均評分
  - `created_at` (DateTime) - 帳號建立時間

**Activity Model** [Source: backend/models/activity.py]
- **Table**: `activities`
- **Primary Key**: `activity_id` (Integer, Auto-increment)
- **相關欄位**:
  - `title` (String(200), NOT NULL) - 活動標題
  - `start_date` (Date, NOT NULL) - 開始日期
  - `location` (String(200), NOT NULL) - 地點
  - `status` (String(50), DEFAULT '招募中') - 狀態
  - `creator_id` (Integer, FK to users) - 創建者 ID
  - `created_at` (DateTime) - 建立時間

**ActivityParticipant Model** [Source: backend/models/activity_participant.py]
- **Table**: `activity_participants`
- **Composite Key**: (`activity_id`, `user_id`)
- **相關欄位**:
  - `status` (String(50)) - 參與狀態 ('pending', 'approved', 'joined', 'rejected')
  - `joined_at` (DateTime) - 加入時間

**Match Model** [Source: backend/models/match.py]
- **Table**: `matches`
- **Primary Key**: `match_id` (Integer, Auto-increment)
- **相關欄位**:
  - `user_a` (Integer, FK to users)
  - `user_b` (Integer, FK to users)
  - `status` (String(50)) - 媒合狀態 ('pending', 'accepted', 'confirmed', 'rejected')

### API Specifications

**Endpoint 1**: `GET /api/users/stats` [Source: backend/blueprints/users.py:11-72]

**Authentication**: Required (JWT Token)

**Request Headers**:
```
Authorization: Bearer <JWT_TOKEN>
```

**Response (Success - 200)**:
```json
{
  "stats": {
    "activities": 15,          // 總活動數（創建的 + 參加的，不重複）
    "matches": 8,              // 媒合成功數（confirmed 或 accepted 狀態）
    "allMatches": 10,          // 所有媒合數（包括 pending，可選）
    "unreadMessages": 0,       // 未讀訊息數（目前為 0，待實作）
    "travelDays": 15,          // 旅遊天數（目前 = activities）
    "reviews": 5               // 評價數量（從 user.rating_count）
  }
}
```

**Response (Error - 401)**:
```json
{
  "msg": "Missing Authorization Header"
}
```

**Response (Error - 500)**:
```json
{
  "error": "<錯誤訊息>"
}
```

**統計計算邏輯** [Source: backend/blueprints/users.py]:
1. **活動數計算**:
   - 創建的活動：`Activity.query.filter_by(creator_id=current_user_id).count()`
   - 參加的活動：`ActivityParticipant.query.filter(status in ['approved', 'joined'], 排除自己創建的).count()`
   - 總活動數 = 創建的 + 參加的（不重複）

2. **媒合數計算**:
   - `Match.query.filter((user_a OR user_b = current_user_id), status in ['confirmed', 'accepted']).count()`

3. **評價數計算**:
   - 從 `User.rating_count` 欄位獲取

---

**Endpoint 2**: `GET /api/users/recent-activities` [Source: backend/blueprints/users.py:74-123]

**Authentication**: Required (JWT Token)

**Request Headers**:
```
Authorization: Bearer <JWT_TOKEN>
```

**Response (Success - 200)**:
```json
{
  "activities": [
    {
      "activity_id": 123,
      "title": "陽明山登山之旅",
      "start_date": "2025-11-15",
      "location": "陽明山",
      "status": "招募中",
      "creator_id": 1,
      "created_at": "2025-11-01T10:30:00",
      "max_participants": 10,
      "current_participants": 3,
      // ...其他活動欄位
    },
    // ...最多 5 個活動
  ]
}
```

**Response (Error - 401)**:
```json
{
  "msg": "Missing Authorization Header"
}
```

**Response (Error - 500)**:
```json
{
  "error": "<錯誤訊息>"
}
```

**查詢邏輯** [Source: backend/blueprints/users.py]:
1. 獲取創建的活動（最新 5 個）：`Activity.query.filter_by(creator_id).order_by(created_at desc).limit(5)`
2. 獲取參加的活動（最新 5 個）：通過 `ActivityParticipant` 查詢 `status in ['approved', 'joined']`
3. 合併兩個列表並去重
4. 按 `created_at` 排序，取前 5 個
5. 使用 `activity.to_dict()` 轉換為 JSON

### Component Specifications

**Dashboard.vue Component** [新建立]

- **Location**: `frontend/src/views/Dashboard.vue`
- **Type**: Page Component (Vue 3 SFC with Composition API)
- **Purpose**: 顯示使用者的個人統計儀表板

**建議結構**:
```vue
<template>
  <div class="dashboard-page">
    <div class="dashboard-container">
      <h1>我的儀表板</h1>
      
      <!-- 統計卡片區 -->
      <div v-if="loading" class="stats-grid">
        <el-skeleton v-for="i in 4" :key="i" animated>
          <template #template>
            <el-skeleton-item variant="rect" style="height: 120px" />
          </template>
        </el-skeleton>
      </div>
      
      <div v-else-if="error" class="error-state">
        <el-result icon="error" title="載入失敗" :sub-title="error">
          <template #extra>
            <el-button type="primary" @click="fetchDashboardData">
              重新載入
            </el-button>
          </template>
        </el-result>
      </div>
      
      <div v-else class="stats-grid">
        <StatCard 
          icon="Calendar" 
          label="總活動數" 
          :value="stats.activities" 
          color="primary"
        />
        <StatCard 
          icon="UserFilled" 
          label="媒合數" 
          :value="stats.matches" 
          color="success"
        />
        <StatCard 
          icon="Star" 
          label="評價數" 
          :value="stats.reviews" 
          color="warning"
        />
        <StatCard 
          v-if="stats.reviews > 0"
          icon="Trophy" 
          label="平均評分" 
          :value="averageRating" 
          color="danger"
        />
      </div>
      
      <!-- 快速操作區 -->
      <div class="quick-actions">
        <h2>快速操作</h2>
        <div class="actions-grid">
          <el-button type="primary" @click="router.push('/activities/create')">
            <el-icon><Plus /></el-icon>
            建立新活動
          </el-button>
          <el-button @click="router.push('/activities')">
            <el-icon><Search /></el-icon>
            瀏覽活動
          </el-button>
          <el-button @click="router.push('/matches')">
            <el-icon><Connection /></el-icon>
            我的媒合
          </el-button>
        </div>
      </div>
      
      <!-- 最近活動區 -->
      <div class="recent-activities">
        <h2>最近活動</h2>
        
        <div v-if="loading" class="activities-list">
          <el-skeleton v-for="i in 3" :key="i" animated>
            <template #template>
              <el-skeleton-item variant="rect" style="height: 100px; margin-bottom: 16px" />
            </template>
          </el-skeleton>
        </div>
        
        <div v-else-if="recentActivities.length === 0" class="empty-state">
          <el-empty description="還沒有任何活動">
            <el-button type="primary" @click="router.push('/activities/create')">
              建立第一個活動
            </el-button>
          </el-empty>
        </div>
        
        <div v-else class="activities-list">
          <div 
            v-for="activity in recentActivities" 
            :key="activity.activity_id"
            class="activity-item"
            @click="router.push(`/activities/${activity.activity_id}`)"
          >
            <div class="activity-header">
              <h3>{{ activity.title }}</h3>
              <el-tag :type="getStatusTagType(activity.status)" size="small">
                {{ activity.status }}
              </el-tag>
            </div>
            <div class="activity-info">
              <span>
                <el-icon><Calendar /></el-icon>
                {{ formatDate(activity.start_date) }}
              </span>
              <span>
                <el-icon><Location /></el-icon>
                {{ activity.location }}
              </span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { getUserStats, getRecentActivities } from '@/api/users'
import { 
  Calendar, Location, UserFilled, Star, Trophy, 
  Plus, Search, Connection 
} from '@element-plus/icons-vue'
import dayjs from 'dayjs'
import StatCard from '@/components/common/StatCard.vue' // 或在此文件內定義

const router = useRouter()
const loading = ref(true)
const error = ref(null)
const stats = ref({
  activities: 0,
  matches: 0,
  reviews: 0,
  travelDays: 0
})
const recentActivities = ref([])

// Computed
const averageRating = computed(() => {
  // 若需要從後端獲取平均評分，可擴展 API
  // 目前僅顯示評價數量
  return stats.value.reviews > 0 ? 'N/A' : 0
})

// Methods
const fetchDashboardData = async () => {
  try {
    loading.value = true
    error.value = null
    
    // 並行獲取統計和最近活動
    const [statsRes, activitiesRes] = await Promise.all([
      getUserStats(),
      getRecentActivities()
    ])
    
    stats.value = statsRes.data.stats
    recentActivities.value = activitiesRes.data.activities
    
  } catch (err) {
    console.error('載入儀表板資料失敗:', err)
    if (err.response?.status === 401) {
      // 認證失效，跳轉至登入頁（由 Axios 攔截器處理）
      return
    }
    error.value = err.response?.data?.error || '載入失敗，請稍後再試'
  } finally {
    loading.value = false
  }
}

const formatDate = (dateStr) => {
  return dayjs(dateStr).format('YYYY-MM-DD')
}

const getStatusTagType = (status) => {
  const statusMap = {
    '招募中': 'success',
    '進行中': 'warning',
    '已完成': 'info',
    '已取消': 'danger'
  }
  return statusMap[status] || 'info'
}

// Lifecycle
onMounted(() => {
  fetchDashboardData()
})
</script>

<style lang="scss" scoped>
.dashboard-page {
  min-height: 100vh;
  background: var(--el-bg-color-page);
  padding: var(--spacing-xl);
}

.dashboard-container {
  max-width: 1200px;
  margin: 0 auto;
  
  h1 {
    font-size: var(--font-size-h1);
    font-weight: var(--font-weight-bold);
    color: var(--el-text-color-primary);
    margin-bottom: var(--spacing-xl);
  }
  
  h2 {
    font-size: var(--font-size-h3);
    font-weight: var(--font-weight-medium);
    color: var(--el-text-color-primary);
    margin-bottom: var(--spacing-md);
  }
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--spacing-lg);
  margin-bottom: var(--spacing-xl);
  
  @media (max-width: 1024px) {
    grid-template-columns: repeat(2, 1fr);
  }
  
  @media (max-width: 640px) {
    grid-template-columns: 1fr;
  }
}

.quick-actions {
  margin-bottom: var(--spacing-xl);
  
  .actions-grid {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }
}

.recent-activities {
  .activities-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }
  
  .activity-item {
    padding: var(--spacing-md);
    background: var(--el-bg-color);
    border-radius: var(--border-radius-md);
    box-shadow: var(--el-box-shadow-light);
    cursor: pointer;
    transition: all 0.3s;
    
    &:hover {
      transform: translateY(-2px);
      box-shadow: var(--el-box-shadow);
    }
    
    .activity-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--spacing-sm);
      
      h3 {
        font-size: var(--font-size-h4);
        font-weight: var(--font-weight-medium);
        color: var(--el-text-color-primary);
        margin: 0;
      }
    }
    
    .activity-info {
      display: flex;
      gap: var(--spacing-md);
      font-size: var(--font-size-small);
      color: var(--el-text-color-secondary);
      
      span {
        display: flex;
        align-items: center;
        gap: var(--spacing-xs);
      }
    }
  }
}

.error-state {
  padding: var(--spacing-xl);
}

.empty-state {
  padding: var(--spacing-xl);
  text-align: center;
}
</style>
```

**StatCard.vue Component** [新建立，可選]

- **Location**: `frontend/src/components/common/StatCard.vue`
- **Type**: Reusable Component
- **Purpose**: 顯示統計卡片

```vue
<template>
  <el-card class="stat-card" :class="`stat-card--${color}`">
    <div class="stat-card__content">
      <div class="stat-card__icon">
        <component :is="iconComponent" />
      </div>
      <div class="stat-card__info">
        <div class="stat-card__value">{{ displayValue }}</div>
        <div class="stat-card__label">{{ label }}</div>
      </div>
    </div>
  </el-card>
</template>

<script setup>
import { computed } from 'vue'
import * as ElementPlusIcons from '@element-plus/icons-vue'

const props = defineProps({
  icon: {
    type: String,
    required: true
  },
  label: {
    type: String,
    required: true
  },
  value: {
    type: [Number, String],
    required: true
  },
  color: {
    type: String,
    default: 'primary',
    validator: (value) => ['primary', 'success', 'warning', 'danger', 'info'].includes(value)
  }
})

const iconComponent = computed(() => {
  return ElementPlusIcons[props.icon]
})

const displayValue = computed(() => {
  if (typeof props.value === 'number') {
    return props.value.toLocaleString()
  }
  return props.value
})
</script>

<style lang="scss" scoped>
.stat-card {
  &__content {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
  }
  
  &__icon {
    font-size: 48px;
    
    .stat-card--primary & {
      color: var(--el-color-primary);
    }
    .stat-card--success & {
      color: var(--el-color-success);
    }
    .stat-card--warning & {
      color: var(--el-color-warning);
    }
    .stat-card--danger & {
      color: var(--el-color-danger);
    }
    .stat-card--info & {
      color: var(--el-color-info);
    }
  }
  
  &__info {
    flex: 1;
  }
  
  &__value {
    font-size: var(--font-size-h2);
    font-weight: var(--font-weight-bold);
    color: var(--el-text-color-primary);
    margin-bottom: var(--spacing-xs);
  }
  
  &__label {
    font-size: var(--font-size-small);
    color: var(--el-text-color-secondary);
  }
}
</style>
```

### File Locations

Based on [Source: architecture/3-專案結構-project-structure.md]:

**Frontend**:
- 儀表板頁面: `frontend/src/views/Dashboard.vue` (新建或更新)
- 統計卡片組件: `frontend/src/components/common/StatCard.vue` (新建，可選)
- Users API 服務: `frontend/src/api/users.js` (擴展 - 添加 getUserStats, getRecentActivities)
- 路由配置: `frontend/src/router/index.js` (確認儀表板路由)
- 前端測試: `frontend/tests/unit/views/Dashboard.spec.js` (新建)

**Backend**:
- 統計端點: `backend/blueprints/users.py` (已存在 get_user_stats, get_recent_activities)
- User Model: `backend/models/user.py`
- Activity Model: `backend/models/activity.py`
- ActivityParticipant Model: `backend/models/activity_participant.py`
- Match Model: `backend/models/match.py`
- 後端測試: `backend/tests/test_users.py` (擴展 - 添加統計和最近活動測試)

### Testing Requirements

Based on [Source: architecture/11-測試策略-testing-requirements.md]:

**Unit Testing (Vitest)**:
- **Location**: `frontend/tests/unit/views/Dashboard.spec.js`
- **Framework**: Vitest + @vue/test-utils + Element Plus
- **Coverage Target**: 80%+
- **Test Cases**:
  1. 頁面正確渲染
  2. 統計卡片顯示正確數據
  3. 最近活動列表渲染
  4. 空狀態顯示（無活動）
  5. 載入狀態顯示（Skeleton）
  6. 錯誤狀態顯示與重新載入
  7. 快速操作按鈕點擊跳轉
  8. 活動卡片點擊跳轉
  9. API 調用（Mock getUserStats, getRecentActivities）
  10. 日期格式化正確
  11. 狀態標籤顏色正確

**Backend Testing (pytest)**:
- **Location**: `backend/tests/test_users.py`
- **Framework**: pytest + pytest-flask
- **Test Cases**:
  - **統計 API 測試**:
    1. 獲取統計數據成功
    2. 統計計算正確性（活動數、媒合數、評價數）
    3. 未認證請求返回 401
    4. 無活動/媒合時返回 0
  - **最近活動 API 測試**:
    1. 獲取最近活動成功
    2. 最多返回 5 個活動
    3. 活動按創建時間排序
    4. 包含創建和參與的活動
    5. 未認證請求返回 401

**Running Tests**:
```bash
# Frontend
npm run test:unit

# Backend
pytest backend/tests/test_users.py -v -k "stats or recent"
```

### Technical Constraints

[Source: architecture/2-前端技術棧-frontend-tech-stack.md + PRD NFR]

1. **Framework Versions**:
   - Vue.js 3.3.8+
   - Element Plus 2.4.2+
   - Pinia 2.1.7+
   - Day.js 1.11.10+ (日期格式化)

2. **Performance Requirements** [NFR1]:
   - API 回應時間 < 500ms
   - 使用 Promise.all() 並行獲取資料以提升載入速度
   - 使用 Skeleton 提供即時視覺反饋

3. **UI/UX Requirements**:
   - 響應式設計：桌面 (4 欄) → 平板 (2 欄) → 手機 (1 欄)
   - 載入狀態使用 Element Plus Skeleton
   - 空狀態使用 Element Plus Empty
   - 錯誤狀態使用 Element Plus Result

4. **Browser Support** [NFR3]:
   - Chrome 90+, Firefox 88+, Safari 14+, Edge 90+

5. **Authentication**:
   - 所有 API 需 JWT 認證
   - 路由需使用 `requiresAuth` meta

### Project Structure Notes

[Source: architecture/3-專案結構-project-structure.md]

**新增目錄/檔案**:
- ✅ `frontend/src/views/Dashboard.vue` - 儀表板頁面（可能已存在，需更新）
- ✅ `frontend/src/components/common/StatCard.vue` - 統計卡片組件（新建，可選）
- ✅ `frontend/tests/unit/views/Dashboard.spec.js` - 單元測試

**修改檔案**:
- ✅ `frontend/src/api/users.js` - 添加 getUserStats, getRecentActivities 函數
- ✅ `frontend/src/router/index.js` - 確認儀表板路由配置
- ✅ `backend/tests/test_users.py` - 添加統計和最近活動測試

**無結構衝突**：所有新增檔案符合現有專案結構。

### Additional Notes

**API 整合重點**:
- 使用 `Promise.all()` 並行獲取統計和最近活動，提升載入速度
- 統計 API 已包含所有需要的數據（activities, matches, reviews）
- 最近活動 API 已實作去重和排序邏輯，前端直接使用即可

**UI/UX 最佳實踐**:
1. **載入狀態**:
   - 使用 Skeleton 提供即時視覺反饋
   - 避免空白畫面造成使用者不安

2. **空狀態**:
   - 提供明確的引導行動（「建立第一個活動」按鈕）
   - 使用友善的文案

3. **錯誤處理**:
   - 區分不同類型的錯誤（網路、伺服器、認證）
   - 提供重新載入功能
   - 認證失效自動跳轉至登入頁

4. **響應式設計**:
   - 使用 CSS Grid 實現靈活佈局
   - 確保小螢幕上的可讀性
   - 測試各種裝置尺寸

**統計數據說明**:
- **活動數**: 創建的活動 + 參加的活動（不重複計算）
- **媒合數**: 只計算 'confirmed' 或 'accepted' 狀態的媒合
- **評價數**: 從 `User.rating_count` 欄位獲取
- **平均評分**: 目前後端未返回，可顯示為 "N/A" 或未來擴展 API

**未來可能的增強**:
1. **統計圖表**:
   - 使用 Chart.js 或 ECharts 顯示活動趨勢
   - 顯示月度/年度統計

2. **個性化推薦**:
   - 根據使用者興趣推薦活動
   - 顯示「您可能感興趣的活動」區塊

3. **成就系統**:
   - 顯示使用者成就徽章
   - 完成特定里程碑獲得獎勵

4. **實時更新**:
   - 使用 Socket.IO 實時更新統計數據
   - 當有新媒合或活動時即時通知

**參考資源**:
- Element Plus Card: https://element-plus.org/en-US/component/card.html
- Element Plus Skeleton: https://element-plus.org/en-US/component/skeleton.html
- Element Plus Empty: https://element-plus.org/en-US/component/empty.html
- Element Plus Result: https://element-plus.org/en-US/component/result.html
- Day.js: https://day.js.org/

## Testing

### Frontend Testing
```bash
# 執行單元測試
npm run test:unit

# 執行帶覆蓋率的測試
npm run test:coverage

# UI 測試模式
npm run test:ui
```

### Backend Testing
```bash
# 執行統計 API 測試
pytest backend/tests/test_users.py::test_get_user_stats -v

# 執行最近活動 API 測試
pytest backend/tests/test_users.py::test_get_recent_activities -v

# 執行所有使用者相關測試
pytest backend/tests/test_users.py -v

# 帶覆蓋率報告
pytest backend/tests/test_users.py --cov=backend/blueprints/users --cov-report=html
```

### Manual Testing Checklist

- [ ] 完整儀表板載入流程（統計 + 最近活動）
- [ ] 統計數據準確性驗證
- [ ] 最近活動列表顯示與排序
- [ ] 活動卡片點擊跳轉
- [ ] 快速操作按鈕跳轉
- [ ] 空狀態顯示（無活動）
- [ ] 載入狀態（Skeleton）
- [ ] 錯誤處理與重新載入
- [ ] 認證保護（未登入跳轉）
- [ ] 響應式設計（桌面/平板/手機）
- [ ] API 回應時間 < 500ms
- [ ] 頁面載入流暢度

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-11-05 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

*此區塊由開發代理在實作時填寫*

### Agent Model Used

(待開發代理填寫)

### Debug Log References

(待開發代理填寫)

### Completion Notes List

(待開發代理填寫)

### File List

(待開發代理填寫)

## QA Results

*此區塊由 QA 代理審查後更新審查結果*
